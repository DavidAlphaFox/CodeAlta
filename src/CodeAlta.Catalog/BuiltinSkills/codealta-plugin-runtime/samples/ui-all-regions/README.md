# UI all-regions sample

Contributes a visual marker for every supported `PluginUiRegion`, session status/footer status items, and a `/ui-sample` command that shows a notification/status message. Copy this sample into a plugin root to verify the currently wired UI insertion points in the live app.
